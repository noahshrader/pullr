$(function () {
    // Custom scrollbars
    $(".campaigns-list-wrap, .site-content").mCustomScrollbar({
        theme:"minimal",
        mouseWheel:{preventDefault:true},
        scrollInertia: 0
    });
});